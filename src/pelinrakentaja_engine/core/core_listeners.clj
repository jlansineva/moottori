(ns pelinrakentaja-engine.core.core-listeners
  (:require [pelinrakentaja-engine.utils.log :as log]))

(defn engine-status
  [state]
  state)

(defn pressed-keys
  [state]
  state)

(defn entity-with-id
  [entity-id]
  )
